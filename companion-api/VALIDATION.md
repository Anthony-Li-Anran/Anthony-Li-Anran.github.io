# 阶段一验收记录 · 2026-09-21

## 最新：GitHub Pages 浏览器直连

- 2026-09-22：API Key 可选择保存在当前标签页的 sessionStorage，刷新和站内换页后恢复，关闭标签页或清除会话后删除；回复解析兼容代码围栏、多行对象、数组、旧 NDJSON、普通文本与部分截断内容，表演字段异常只回退为自然状态。

- 已移除邀请码 UI 和前端后端地址配置，不恢复旧邀请码凭证，不使用 Anthony 的额度。
- 8 项 Node 测试覆盖分片 UTF-8/SSE、固定服务商目标、不发送密钥到静态资料、未知服务商拒绝、401 不回退、断流与取消、近况边界、检索与梗去重。
- 用网站 Origin 对两个官方接口发起 OPTIONS 预检，两者均允许 POST、Authorization 和 Content-Type。
- 在本地页面用浏览器与无效测试 Key 分别请求 DeepSeek／千问，两者均得到可读 401 响应，证明当时跨域请求可达；没有用真实 Key 生成或消耗付费额度。
- 浏览器模拟 SSE 验证回复与表演 UI，通过；请求只有静态资料与 DeepSeek 官方地址，无 `/api/chat`、`/api/redeem` 或 localhost:8787，测试 Key 未持久保存。
- 下方为先前后端模式验收记录。邀请码后端保留但当前网页不调用，真实模型生成质量仍待真实账户验收。

## 已通过

- Python 后端 10 项自动化测试：邀请码单次兑换、撤销、过期、并发原子预留、全站和个人额度、未知用量保守计费、限流、API Key 不回显、固定上游、来源与请求体限制、流式回答与用量结算、异常断流、表演指令允许列表、近况七天边界／相关性／查询失败、资料检索与梗去重。
- Jekyll 构建成功，生成 23 条公开页面记录；输出目录不包含后端代码、数据库或密钥配置。
- 浏览器实际加载模型，通过本地模拟上游验证自带 API 与邀请码两条聊天链路；真实 Key 未使用。
- 逐句显示、思考与表情动作事件、停止、清除会话、隐藏／恢复、无凭证页面介绍均通过。
- API Key 未进入 sessionStorage 或 localStorage；同标签页导航后恢复对话和邀请码凭证，正确触发新页面介绍。
- 桌面浅色／深色页面视觉检查通过；小屏 390px 时看板娘隐藏，模型资源请求为 0。
- 本机后端健康检查：ready=true，knowledge_ready=true，invite_enabled=false。

截图和浏览器测试脚本保存在仓库忽略目录 `local/stelle-ai-*`、`local/verify-stelle-*.js`；这些不是网站发布内容。

## 尚待真实账户与主人验收

- DeepSeek／千问实际鉴权、具体模型的逐行 JSON 输出稳定性、响应速度和实际费用。
- 星的人设、表情强度、文章回答与玩梗质量需要 Anthony 真实试聊后调整。
- 公开近况当前为空，需 Anthony 填入真实记录；梗库是小型起步集。
- 未部署后端，未填写生产 API 地址，未上传网站；邀请码消费仍默认关闭。
- 语音、浏览器本地语言模型与跨会话访客记忆不在阶段一实现范围内。
